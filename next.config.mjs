/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote image hosts here once real assets/CDN are connected.
    remotePatterns: [],
  },
};

export default nextConfig;
